proxy_listen = 0.0.0.0:8000, 0.0.0.0:8443 ssl
admin_listen = 0.0.0.0:8001, 0.0.0.0:8444 ssl

# enabled plugins
plugins = bundled, hide-token, oidc, jwt, request-transformer, key-auth

# database config
database = postgres
pg_database = kong
